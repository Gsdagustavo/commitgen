# Commitgen

This is a simple commit AI powered commit generator, made using Golang. The commit is generated based on a projects's diff of the latest changes on a local repository.

Currently under development.
